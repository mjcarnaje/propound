export enum CollectionNames {
  STUDENTS = "students",
  TEACHERS = "teachers",
  ACTIVITIES = "activities",
}

export enum ActivityCollectionNames {
  GAMES = "games",
  MATERIALS = "materials",
  STUDENTS = "students",
}

export enum StudentCollectionNames {
  RESULTS = "results",
}
